{% extends 'base.html.twig' %}

{% block title %}Liste Utilisateurs{% endblock %}

{% block body %}

    <div class="container mt-5">
    <div class="row justify-content-center">
    <div class="fondProfil">

    <h1 class="mb-3 text-center categorie">Liste Utilisateurs</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Actif</th>
            <th>Supprimer l'utilisateur</th>
        </tr>
        </thead>
        <tbody>
        {% for participant in participants %}
            {% if userConnected != participant %}
                <tr>
                    <td>{{ participant.nom }}</td>
                    <td>{{ participant.prenom }}</td>
                    <td>
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckChecked" checked="" name="remember_me">

                        </div>
                    </td>
                    <td><button type="button" class="custom-btn-an">Supprimer</button></td>
                </tr>
            {% endif %}

        {% endfor %}
        </tbody>
    </table>
    </div>
    </div>
    </div>

{% endblock %}